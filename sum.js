/**
 * This function takes two numbers and return their sum.
 * @param {int} a First input number
 * @param {int} b Second input number
 * @returns {int} Sum of the two numbers
 */
function sum(a, b) {
  return a + b;
}
export default function sum(a, b) {
  return a + b;
}